Queue vs List

Priority Queue

KNOW THY COMPLEXITIES
### Hash Map

Mod hashing
Chaining (state after insert/remove)
Linear Probe (state after Insert/remove)
Load Factor
### Trees

Keywords

- Ancestor
- Predecessor
- Internal Node
- Leaf Node

What makes a valid BST? For each subtree, nodes that are less than their parent should be on the left, nodes that are greater than their parent should be on the right. A BST should have 0-2 children per node.

How do we determine how many comparisons it takes in the worst case to find an element in a BST?

Be familiar with BST traversal

BST Node Removal (Be familiar with the 3 cases to consider when removing a node and how we accomplish it)

Inorder, preorder, postorder traversal

Self-balancing BSTs are still BSTs which means they have to adhere to the rules of being a BST. So if they're not a BST, they're not a self balancing tree, ie red-black tree.

A binary tree is not a BST, but a BST is a binary tree.

### Max/Min Heaps

Insert/Remove
Array to heap

### Graphs

Know how to perform Dijkstra's Shortest Path

Adjacency List (T(n) and S(n))

Adjaceny Matrix (T(n) and S(n))

Matrix to Graph

BFS vs DFS for graphs

Topological Sort