DAG comes from Directed Acyclic Graph but Apache's DAG concept is more than that.

In Apache Airflow, their DAG library provides Scheduling, Tasks, Task Dependencies, Callbacks, and additional parameters.

It has functionality similar to cron jobs because is focused on dependencies and ordering.